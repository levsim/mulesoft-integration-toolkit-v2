CREATE TABLE IF NOT EXISTS traditions (
    ID INT PRIMARY KEY,
    City VARCHAR(255),
    Tradition VARCHAR(255)
);
-- Optional: Add some sample data
-- INSERT INTO traditions (ID, City, Tradition) VALUES (1, 'TestCity', 'TestTraditionFromDocker');
